# Linux

## Install 

sudo apt install -y \
          build-essential \
          autoconf libtool pkg-config \
          git cmake libc-ares-dev libre2-dev libssl-dev \
          zlib1g-dev curl

* https://github.com/faaxm/exmpl-cmake-grpc/blob/master/.github/workflows/build.yml


* vcpkg install grpc (1.51.1)
* vcpkg install protobuf 

# CMAKE
set (VCPKG_HOME "/DEV/vcpkg")