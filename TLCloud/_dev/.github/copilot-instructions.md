# Copilot Instructions

## Project Guidelines
- User prefers that deployment clusters are unique by name and must be created together with a ClusterVault using caller-provided vault details; if vault creation fails, the cluster should not persist.