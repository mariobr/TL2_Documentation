# Prompt to create solution dependency overview

- Create or update a file named `solution.dependency.overview.md` that contains:
- A title: "TLCloud - Project Dependency Graph"
- One sentence explaining it shows project-to-project dependencies and shared libraries.
- A single Mermaid flowchart (orientation LR) that visualizes the projects and dependencies of the solution tlcloud.sln below.
- omit tlcloud from the chard
- Color-coded node classes per category and styled links. 
- Output only the final markdown file content with the mermaid block; no extra commentary.
- Exclude project _PlayGround